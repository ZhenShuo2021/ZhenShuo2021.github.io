# -- Markup --
# These settings are required for the theme to function.

[goldmark]
[goldmark.renderer]
  unsafe = true


[highlight]
  noClasses = false
  codeFences = false

[tableOfContents]
  startLevel = 1
  endLevel = 5 # 不包含 endLevel，所以只显示 1-2 级别。
